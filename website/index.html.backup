<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Warelay Agent - Multi-Platform Messaging Automation</title>
    <meta name="description" content="Automate WhatsApp and Twitter with AI-powered responses. Built on the warelay framework.">
    <link rel="icon" type="image/svg+xml" href="favicon.svg">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary: #00f3ff;
            --primary-dark: #00d4e0;
            --secondary: #ff006e;
            --dark: #0a0e1a;
            --light: #0f1419;
            --gray: #4a5568;
            --border: #1a202c;
            --neon-cyan: #00f3ff;
            --neon-pink: #ff006e;
            --neon-purple: #b026ff;
        }

        body {
            font-family: 'Courier New', 'Consolas', monospace;
            line-height: 1.6;
            color: var(--neon-cyan);
            background: var(--dark);
            background-image: 
                repeating-linear-gradient(0deg, rgba(0, 243, 255, 0.03) 0px, transparent 1px, transparent 2px, rgba(0, 243, 255, 0.03) 3px),
                repeating-linear-gradient(90deg, rgba(0, 243, 255, 0.03) 0px, transparent 1px, transparent 2px, rgba(0, 243, 255, 0.03) 3px);
            background-size: 40px 40px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 2rem;
        }

        /* Header */
        header {
            background: rgba(10, 14, 26, 0.95);
            border-bottom: 2px solid var(--neon-cyan);
            padding: 1rem 0;
            position: sticky;
            top: 0;
            z-index: 100;
            box-shadow: 0 0 20px rgba(0, 243, 255, 0.3);
            backdrop-filter: blur(10px);
        }

        nav {
            display: flex;
            justify-content: space-between;
            align-items: center;
        }

        .logo {
            font-size: 1.5rem;
            font-weight: 700;
            color: var(--primary);
            text-decoration: none;
            display: flex;
            align-items: center;
            gap: 0.5rem;
        }

        .logo img {
            height: 40px;
            width: 40px;
        }

        .nav-links {
            display: flex;
            gap: 2rem;
            list-style: none;
        }

        .nav-links a {
            color: var(--neon-cyan);
            text-decoration: none;
            font-weight: 500;
            transition: all 0.3s;
            text-shadow: 0 0 5px rgba(0, 243, 255, 0.5);
        }

        .nav-links a:hover {
            color: var(--neon-pink);
            text-shadow: 0 0 10px rgba(255, 0, 110, 0.8);
        }

        /* Hero */
        .hero {
            padding: 6rem 0;
            text-align: center;
            background: linear-gradient(135deg, rgba(176, 38, 255, 0.2) 0%, rgba(0, 243, 255, 0.2) 100%),
                        url('data:image/svg+xml,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 100 100"><text y="50" font-size="100" opacity="0.02">⚡</text></svg>');
            color: var(--neon-cyan);
            border-bottom: 2px solid var(--neon-cyan);
            box-shadow: inset 0 0 100px rgba(0, 243, 255, 0.1);
            position: relative;
        }
        
        .hero::before {
            content: "// AUTHORIZED ACCESS ONLY";
            position: absolute;
            top: 1rem;
            right: 2rem;
            font-size: 0.7rem;
            color: var(--neon-pink);
            opacity: 0.6;
            font-family: 'Courier New', monospace;
        }

        .hero h1 {
            font-size: 3.5rem;
            margin-bottom: 1rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            text-shadow: 0 0 20px rgba(0, 243, 255, 0.8),
                         0 0 40px rgba(176, 38, 255, 0.5);
            animation: glitch 3s infinite;
        }

        @keyframes glitch {
            0%, 90%, 100% { transform: translate(0); }
            92% { transform: translate(-2px, 1px); }
            94% { transform: translate(2px, -1px); }
        }

        .hero p {
            font-size: 1.3rem;
            margin-bottom: 2rem;
            opacity: 0.9;
            color: var(--gray);
            font-family: 'Courier New', monospace;
        }
        
        .hero p::before {
            content: "> ";
            color: var(--neon-pink);
        }

        .cta-buttons {
            display: flex;
            gap: 1rem;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 1rem 2rem;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.3s;
            border: 2px solid transparent;
            cursor: pointer;
        }

        .btn-primary {
            background: transparent;
            color: var(--neon-cyan);
            border: 2px solid var(--neon-cyan);
            box-shadow: 0 0 20px rgba(0, 243, 255, 0.3);
        }

        .btn-primary:hover {
            background: var(--neon-cyan);
            color: var(--dark);
            transform: translateY(-2px);
            box-shadow: 0 0 30px rgba(0, 243, 255, 0.8);
        }

        .btn-secondary {
            background: transparent;
            color: var(--neon-pink);
            border: 2px solid var(--neon-pink);
            box-shadow: 0 0 20px rgba(255, 0, 110, 0.3);
        }

        .btn-secondary:hover {
            background: var(--neon-pink);
            color: var(--dark);
            box-shadow: 0 0 30px rgba(255, 0, 110, 0.8);
        }

        /* Features */
        .features {
            padding: 4rem 0;
            background: rgba(15, 20, 25, 0.8);
            backdrop-filter: blur(5px);
        }

        .section-title {
            text-align: center;
            font-size: 2.5rem;
            margin-bottom: 3rem;
            color: var(--neon-cyan);
            text-transform: uppercase;
            letter-spacing: 0.15em;
            text-shadow: 0 0 10px rgba(0, 243, 255, 0.6);
        }
        
        .section-title::before {
            content: "[ ";
            color: var(--neon-pink);
        }
        
        .section-title::after {
            content: " ]";
            color: var(--neon-pink);
        }

        .feature-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
        }

        .feature-card {
            padding: 2rem;
            border: 2px solid var(--border);
            border-radius: 0;
            transition: all 0.3s;
            background: rgba(10, 14, 26, 0.6);
            position: relative;
            overflow: hidden;
        }
        
        .feature-card::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 2px;
            background: linear-gradient(90deg, var(--neon-cyan), var(--neon-pink));
            opacity: 0;
            transition: opacity 0.3s;
        }

        .feature-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 0 30px rgba(0, 243, 255, 0.3);
            border-color: var(--neon-cyan);
        }
        
        .feature-card:hover::before {
            opacity: 1;
        }

        .feature-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .feature-card h3 {
            font-size: 1.5rem;
            margin-bottom: 1rem;
            color: var(--neon-cyan);
            text-transform: uppercase;
            letter-spacing: 0.05em;
            font-weight: 700;
        }

        .feature-card p {
            color: var(--gray);
            font-family: 'Courier New', monospace;
            font-size: 0.95rem;
        }

        /* Platforms */
        .platforms {
            padding: 4rem 0;
            background: var(--dark);
            border-top: 2px solid var(--neon-pink);
            border-bottom: 2px solid var(--neon-pink);
        }

        .platform-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 2rem;
        }

        .platform-card {
            background: rgba(10, 14, 26, 0.8);
            padding: 2rem;
            border-radius: 0;
            text-align: center;
            border: 2px solid var(--border);
            transition: all 0.3s;
            position: relative;
        }

        .platform-card:hover {
            border-color: var(--neon-cyan);
            transform: scale(1.05);
            box-shadow: 0 0 30px rgba(0, 243, 255, 0.4);
        }

        .platform-card.active {
            border-color: var(--neon-cyan);
            background: rgba(0, 243, 255, 0.1);
            color: var(--neon-cyan);
            box-shadow: 0 0 20px rgba(0, 243, 255, 0.3);
        }
        
        .platform-card.active::after {
            content: "● ONLINE";
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            font-size: 0.7rem;
            color: #00ff00;
            text-shadow: 0 0 5px #00ff00;
        }

        .platform-card h3 {
            font-size: 1.8rem;
            margin-bottom: 0.5rem;
        }

        .platform-card .badge {
            display: inline-block;
            padding: 0.25rem 0.75rem;
            border-radius: 12px;
            font-size: 0.8rem;
            font-weight: 600;
            margin-top: 0.5rem;
        }

        .badge-active {
            background: var(--secondary);
            color: white;
        }

        .badge-soon {
            background: var(--gray);
            color: white;
        }

        /* Code */
        .code-section {
            padding: 4rem 0;
            background: rgba(15, 20, 25, 0.8);
        }
        
        .code-section h3 {
            color: var(--neon-pink);
            text-transform: uppercase;
            letter-spacing: 0.1em;
        }
        
        .code-section h3::before {
            content: "// ";
            color: var(--gray);
        }

        .code-example {
            background: rgba(10, 14, 26, 0.95);
            color: var(--neon-cyan);
            padding: 2rem;
            border-radius: 0;
            overflow-x: auto;
            font-family: 'Monaco', 'Courier New', monospace;
            font-size: 0.9rem;
            line-height: 1.8;
            border: 2px solid var(--neon-cyan);
            box-shadow: inset 0 0 20px rgba(0, 243, 255, 0.1);
            position: relative;
        }
        
        .code-example::before {
            content: "[ TERMINAL ]";
            position: absolute;
            top: 0.5rem;
            right: 0.5rem;
            font-size: 0.7rem;
            color: var(--neon-pink);
            opacity: 0.6;
        }

        .code-example .comment {
            color: #6b7280;
        }

        .code-example .command {
            color: var(--neon-pink);
            text-shadow: 0 0 5px rgba(255, 0, 110, 0.5);
        }

        /* CTA */
        .cta-section {
            padding: 4rem 0;
            background: linear-gradient(135deg, rgba(176, 38, 255, 0.3) 0%, rgba(0, 243, 255, 0.3) 100%);
            color: var(--neon-cyan);
            text-align: center;
            border-top: 2px solid var(--neon-cyan);
            position: relative;
        }
        
        .cta-section::before {
            content: "You won't see me coming — but you'll know when I was there.";
            position: absolute;
            top: 1rem;
            left: 50%;
            transform: translateX(-50%);
            font-size: 0.8rem;
            color: var(--neon-pink);
            opacity: 0.7;
            font-style: italic;
            font-family: 'Courier New', monospace;
        }

        .cta-section h2 {
            font-size: 2.5rem;
            margin-bottom: 1rem;
            text-transform: uppercase;
            letter-spacing: 0.1em;
            text-shadow: 0 0 20px rgba(0, 243, 255, 0.6);
        }

        .cta-section p {
            font-size: 1.2rem;
            margin-bottom: 2rem;
            opacity: 0.9;
            font-family: 'Courier New', monospace;
        }

        /* Footer */
        footer {
            padding: 2rem 0;
            background: rgba(10, 14, 26, 0.98);
            color: var(--gray);
            text-align: center;
            border-top: 2px solid var(--neon-cyan);
            font-family: 'Courier New', monospace;
            font-size: 0.9rem;
        }

        footer a {
            color: var(--neon-cyan);
            text-decoration: none;
            text-shadow: 0 0 5px rgba(0, 243, 255, 0.5);
        }
        
        footer a:hover {
            color: var(--neon-pink);
            text-shadow: 0 0 10px rgba(255, 0, 110, 0.8);
        }
        
        footer::before {
            content: "// CODE NAME: SPECTER";
            display: block;
            color: var(--neon-pink);
            opacity: 0.5;
            font-size: 0.8rem;
            margin-bottom: 1rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .hero h1 {
                font-size: 2.5rem;
            }

            .nav-links {
                gap: 1rem;
            }

            .feature-grid,
            .platform-grid {
                grid-template-columns: 1fr;
            }
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav class="container">
            <a href="#" class="logo">
                <img src="logo.svg" alt="Warelay Agent Logo">
                Warelay Agent
            </a>
            <ul class="nav-links">
                <li><a href="#features">Features</a></li>
                <li><a href="#platforms">Platforms</a></li>
                <li><a href="#docs">Docs</a></li>
                <li><a href="https://github.com/steipete/warelay" target="_blank">GitHub</a></li>
            </ul>
        </nav>
    </header>

    <!-- Hero -->
    <section class="hero">
        <div class="container">
            <h1>Warelay Agent: Specter</h1>
            <p>Ghost in the system. Multi-platform infiltration. AI-driven intelligence. You won't see it coming.</p>
            <div class="cta-buttons">
                <a href="#docs" class="btn btn-primary">[ INITIALIZE ]</a>
                <a href="https://github.com/steipete/warelay" class="btn btn-secondary">[ ACCESS REPO ]</a>
            </div>
        </div>
    </section>

    <!-- Features -->
    <section id="features" class="features">
        <div class="container">
            <h2 class="section-title">Powerful Features</h2>
            <div class="feature-grid">
                <div class="feature-card">
                    <div class="feature-icon">📱</div>
                    <h3>Multi-Platform</h3>
                    <p>Support for WhatsApp (Twilio + Web) and Twitter. More platforms coming soon.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🤖</div>
                    <h3>AI-Powered</h3>
                    <p>Integrate with Claude, GPT, or any CLI tool for intelligent auto-replies.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">💾</div>
                    <h3>Session Memory</h3>
                    <p>Multi-turn conversations with context. Per-user or global sessions.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🎯</div>
                    <h3>Smart Filtering</h3>
                    <p>Allow/block lists, rate limiting, and custom rules per platform.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">⚡</div>
                    <h3>Production Ready</h3>
                    <p>Error handling, state persistence, logging, and deployment guides.</p>
                </div>
                <div class="feature-card">
                    <div class="feature-icon">🔧</div>
                    <h3>Extensible</h3>
                    <p>Easy to add new platforms. Clean provider pattern and CLI framework.</p>
                </div>
            </div>
        </div>
    </section>

    <!-- Platforms -->
    <section id="platforms" class="platforms">
        <div class="container">
            <h2 class="section-title">Supported Platforms</h2>
            <div class="platform-grid">
                <div class="platform-card active">
                    <h3>💬 WhatsApp</h3>
                    <p>Twilio API & WhatsApp Web</p>
                    <span class="badge badge-active">Active</span>
                </div>
                <div class="platform-card active">
                    <h3>🐦 Twitter/X</h3>
                    <p>Twitter API v2</p>
                    <span class="badge badge-active">Active</span>
                </div>
                <div class="platform-card">
                    <h3>💬 Discord</h3>
                    <p>Discord Bot API</p>
                    <span class="badge badge-soon">Coming Soon</span>
                </div>
                <div class="platform-card">
                    <h3>💼 Slack</h3>
                    <p>Slack API</p>
                    <span class="badge badge-soon">Coming Soon</span>
                </div>
            </div>
        </div>
    </section>

    <!-- Code Examples -->
    <section id="docs" class="code-section">
        <div class="container">
            <h2 class="section-title">Quick Start</h2>
            
            <h3 style="margin: 2rem 0 1rem;">WhatsApp Automation</h3>
            <div class="code-example">
<span class="comment"># Send WhatsApp message</span>
<span class="command">npm run warelay send --to +1234567890 --message "Hello!"</span>

<span class="comment"># Start auto-reply relay</span>
<span class="command">npm run warelay relay --provider web --verbose</span>

<span class="comment"># Setup webhook with Tailscale</span>
<span class="command">npm run warelay webhook --ingress tailscale</span>
            </div>

            <h3 style="margin: 2rem 0 1rem;">Twitter Automation</h3>
            <div class="code-example">
<span class="comment"># Post a tweet</span>
<span class="command">npm run warelay-agent tweet --text "Hello from Warelay! 🤖"</span>

<span class="comment"># Send a DM</span>
<span class="command">npm run warelay-agent dm --to username --message "Hi!"</span>

<span class="comment"># Start monitoring & auto-reply</span>
<span class="command">npm run warelay-agent relay --verbose</span>

<span class="comment"># Check account status</span>
<span class="command">npm run warelay-agent status --verbose</span>
            </div>

            <h3 style="margin: 2rem 0 1rem;">Configuration Example</h3>
            <div class="code-example">
{
  <span class="comment">// Monitor settings</span>
  "monitorDMs": true,
  "monitorMentions": true,
  
  <span class="comment">// Auto-reply with AI</span>
  "autoReplyDMs": true,
  "autoReply": {
    "mode": "command",
    "command": ["claude", "--dangerously-skip-permissions"],
    "bodyPrefix": "You are a helpful assistant.\\n\\n",
    "session": {
      "scope": "per-user",
      "resetTriggers": ["/new"],
      "idleMinutes": 60
    }
  }
}
            </div>
        </div>
    </section>

    <!-- CTA -->
    <section class="cta-section">
        <div class="container">
            <h2>Ready to Deploy?</h2>
            <p>Activate Warelay Agent. Leave no trace. Extract what matters.</p>
            <div class="cta-buttons">
                <a href="https://github.com/steipete/warelay" class="btn btn-primary">[ DEPLOY AGENT ]</a>
                <a href="#" class="btn btn-secondary">[ READ DOCS ]</a>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <p>Built on the <a href="https://github.com/steipete/warelay">warelay</a> framework • MIT License</p>
            <p>Maintained by the Ashnet • Enhanced by Nyra Voss</p>
            <p style="margin-top: 1rem; opacity: 0.5; font-size: 0.85rem;">System Status: OPERATIONAL • Neural Link: ACTIVE • Uptime: 99.7%</p>
        </div>
    </footer>

    <script>
        // Smooth scrolling
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });
    </script>
</body>
</html>
